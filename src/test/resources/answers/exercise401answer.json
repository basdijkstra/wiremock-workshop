{
    "request": {
        "method": "GET",
        "url": "/echo-port"
    },
    "response": {
        "status": 200,
        "body": "Listening on port {{request.requestLine.port}}",
        "transformers": ["response-template"]
    }
}